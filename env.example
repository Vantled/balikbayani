# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=balikbayani
DB_USER=postgres
DB_PASSWORD=balikbayani123
# SSL Configuration (optional)
# DB_SSL=true - Enable SSL connection (requires DB_SSL_REQUIRED=true or DB_SSL=true)
# DB_SSL=false - Explicitly disable SSL (default)
# DB_SSL_REQUIRED=true - Enable SSL for production (alternative to DB_SSL=true)

# Application Configuration
NEXTAUwTH_SECRET=balikbayani-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Backup Configuration
# System prefix used in backup filenames: [system]-[YYYYMMDD]-[HHMMSS].ext
SYSTEM_NAME=balikbayani
# Secret required by POST /api/backups/auto via header X-Cron-Secret
CRON_SECRET=change-me-strong-secret
# Optional: explicit paths if pg_dump/tar aren't on PATH
PG_DUMP_PATH=
TAR_PATH=
PSQL_PATH=

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=10485760 # 10MB in bytes

# Security Configuration
JWT_SECRET=balikbayani-jwt-secret-change-in-production
BCRYPT_ROUNDS=12

# Default User Passwords
ADMIN_PASSWORD=admin123
SUPERADMIN_PASSWORD=superadmin123

# Email Configuration (for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_email_password

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# Development Configuration
NODE_ENV=development
